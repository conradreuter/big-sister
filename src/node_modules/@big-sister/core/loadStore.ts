import { PluginConfiguration, Store } from '@big-sister'
import loadPlugin from './utility/loadPlugin'

export default function loadStore(configuration: PluginConfiguration<any>): Promise<Store> {
  return loadPlugin(configuration.plugin, 'store')(configuration.options)
}
