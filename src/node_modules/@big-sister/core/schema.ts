import { makeExecutableSchema } from 'graphql-tools'

/**
 * The root GraphQL schema.
 */
export default makeExecutableSchema({
  typeDefs: `
    # Scalar type that accepts anything
    scalar Any

    # The schema contains the following queries:
    type Query {
      test: Any
    }
  `
})
