import { Spy, SpyConfiguration } from '@big-sister'
import loadPlugin from './utility/loadPlugin'

export default function loadSpy(configuration: SpyConfiguration<any>): Promise<Spy> {
  return loadPlugin(configuration.plugin, 'spy')(configuration.options)
}
