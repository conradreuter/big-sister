import { Store, StoreConfiguration } from '@big-sister/types'
import loadPlugin from './loadPlugin'

export default function createStore(configuration: StoreConfiguration<any>): Promise<Store> {
  const { options, plugin } = configuration
  return loadPlugin(plugin, 'store')(options)
}
