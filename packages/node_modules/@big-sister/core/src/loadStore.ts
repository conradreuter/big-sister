import { Store, StoreConfiguration } from '@big-sister/types'
import loadPlugin from './utility/loadPlugin'

export default function loadStore(configuration: StoreConfiguration<any>): Promise<Store> {
  return loadPlugin(configuration.plugin, 'store')(configuration.options)
}
