import { Spy, SpyConfiguration } from '@big-sister/types'
import loadPlugin from './loadPlugin'

export default function createSpy(configuration: SpyConfiguration<any>): Promise<Spy> {
  const { options, plugin } = configuration
  return loadPlugin(plugin, 'spy')(options)
}
